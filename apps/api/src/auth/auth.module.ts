import { Module } from '@nestjs/common';

/**
 * Signup/login (argon2), nonce infrastructure (issue/verify/burn),
 * wallet verification + SIWS-style wallet login, session & API-key guards.
 */
@Module({})
export class AuthModule {}
